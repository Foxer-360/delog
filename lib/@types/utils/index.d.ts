/**
 * Simple helper to check, if window object exists
 *
 * @return {boolean}
 */
declare const isWindow: () => boolean;
/**
 * Simple helper to check if global object exists
 *
 * @return {boolean}
 */
declare const isGlobal: () => boolean;
export { isGlobal, isWindow, };
